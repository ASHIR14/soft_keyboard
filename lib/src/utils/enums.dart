enum SpecialKey {
  capsLock,
  backspace,
  space,
  enter,
  symbols,
}

enum Capitalization {
  lowerCase,
  upperCase,
  onlyFirstLetter,
}

enum KeyBoardType {
  alphanumeric,
  numeric,
  symbols,
}
