name: soft_keyboard
description: A customisable soft keyboard package.
version: 0.1.0

homepage: https://github.com/ASHIR14/soft_keyboard
repository: https://github.com/ASHIR14/soft_keyboard

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

platforms:
  android:
  ios: